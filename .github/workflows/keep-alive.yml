name: Keep Alive — Diagnóstico Engrene

on:
  schedule:
    # Pinga a cada 5 minutos (limite mínimo do GitHub Actions)
    - cron: '*/5 * * * *'
  workflow_dispatch: # permite rodar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 https://diagnostico-engrene-production.up.railway.app/health)
          echo "Status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "⚠️ Site retornou $STATUS"
            sleep 15
            STATUS2=$(curl -s -o /dev/null -w "%{http_code}" --max-time 30 https://diagnostico-engrene-production.up.railway.app/health)
            echo "Status (retry): $STATUS2"
            if [ "$STATUS2" != "200" ]; then
              echo "❌ Site fora do ar após retry"
              exit 1
            fi
          fi
          echo "✅ Site no ar"
